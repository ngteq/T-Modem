# Contributing / branch model

Locked layout — see **[BRANCHES.md](BRANCHES.md)**.

| Branch | Use |
|--------|-----|
| **`c12s24`** | All T-Modem-c12s24 product work |
| **`main`** | Release + branch **index** only |
| Future topic branches | From `c12s24`; merge back to `c12s24`; list on `main` when lasting |

```bash
git checkout c12s24
```

**AS-IS** · GPLv3 · standard reference only.
